#include "main.h"
/**
 *_abs - prints Write a function that prints the sign of a number.
 *@i: number targeted
 *Return: returns 0
 */
int print_sign(int n)
{
	if ( n > 0)
	return (n);
	else if (i < 0)
	return (-n);
	else
	return (0);


}
