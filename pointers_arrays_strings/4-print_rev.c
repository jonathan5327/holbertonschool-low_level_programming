#include "main.h"
/**
 * print_rev - Write a function that prints a string, in reverse, followed by a
 * new line.
 *
 * @i: character
 */
void print_rev(char *str)
{
	int i = 0;

	while (s[i] != 0)
	{
		i++;
	}
	while (i > 0);
	{
		i--:
		_putchar(s[i]);
	}
	
	_putchar('\n')


}
